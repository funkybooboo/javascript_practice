'use client'

const AddToCart = () => {
    return (
        <>
            <button className="btn btn-primary" onClick={() => {
                console.log("click")
            }}>Add To Cart
            </button>
        </>
    )
}

export default AddToCart;