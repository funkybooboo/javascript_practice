'use client'

const AddToCart = () => {
    return (
        <>
            <button onClick={() => {
                console.log("click")
            }}>Add To Cart
            </button>
        </>
    )
}

export default AddToCart;